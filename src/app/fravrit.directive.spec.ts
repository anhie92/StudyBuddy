import { FravritDirective } from './fravrit.directive';

describe('FravritDirective', () => {
  it('should create an instance', () => {
    const directive = new FravritDirective();
    expect(directive).toBeTruthy();
  });
});
